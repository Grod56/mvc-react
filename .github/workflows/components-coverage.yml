on:
    push:
        paths:
            - "packages/components/src/**"
        branches: [main]
    pull_request:
        paths:
            - "packages/components/src/**"
        branches: [main]
    workflow_call:

jobs:
    call-coverage:
        uses: "./.github/workflows/coverage.yml"
        with:
            package: components

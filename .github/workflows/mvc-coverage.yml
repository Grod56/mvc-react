on:
    push:
        paths:
            - "packages/mvc/src/**"
        branches: [main]
    pull_request:
        paths:
            - "packages/mvc/src/**"
        branches: [main]
    workflow_call:

jobs:
    call-coverage:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: "./.github/workflows/coverage.yml"
              with:
                  package: mvc

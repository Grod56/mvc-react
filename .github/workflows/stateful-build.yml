name: "@mvc-react/stateful build"

on:
    push:
        paths:
            - "packages/stateful/**"
        branches: [main]
    pull_request:
        paths:
            - "packages/stateful/**"
        branches: [main]

jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v1
              with:
                  node-version: "22"

            - name: Clean Install
              run: npm ci -w=packages/stateful

            - name: Run Build
              run: npm run build -w=packages/stateful

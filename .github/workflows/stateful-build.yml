name: build
on:
    push:
        paths:
            - "packages/stateful/**"
        branches: [main]
    pull_request:
        paths:
            - "packages/stateful/**"
        branches: [main]

jobs:
    call-build:
        uses: ./.github/workflows/build.yml
        with:
            package: stateful

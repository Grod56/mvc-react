name: build
on:
    push:
        paths:
            - "packages/mvc/**"
        branches:
            - "**"
    pull_request:
        paths:
            - "packages/mvc/**"
        branches: [main]
    workflow_call:

jobs:
    call-build:
        name: "'mvc' build"
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v6
            - uses: actions/setup-node@v6
              with:
                  node-version: "22"

            - uses: ./.github/actions/build/
              with:
                  package: mvc

name: build
on:
    push:
        paths:
            - "packages/mvc/**"
        branches: [main]
    pull_request:
        paths:
            - "packages/mvc/**"
        branches: [main]

jobs:
    call-build:
        uses: ./.github/workflows/build.yml
        with:
            package: mvc

name: "build action"

inputs:
    package:
        required: true
        type: string

runs:
    using: "composite"
    defaults:
        run:
            working-directory: ./packages/${{ inputs.package }}
    steps:
        - name: Install Dependencies
          run: npm ci --workspaces=false

        - name: Run Tests
          run: npm run test

        - name: Run Build
          run: npm run build
